<h1>New reviews</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>

<%= f.input :content, placeholder: "review" %>

<%= f.input :rating, placeholder: "0 - 5" %>

<%= f.button :submit, label: "Add a new review" %>
<% end %>
